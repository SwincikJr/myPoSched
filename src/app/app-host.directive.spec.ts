import { AppHostDirective } from './app-host.directive';

describe('AppHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
